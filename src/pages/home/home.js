import Button from "../../components/button/button";
import El from "../../utils/El";

function Home(){
    console.log(history)
    return El({
        element : "div",
        child : "home page"
    })
}

export default Home